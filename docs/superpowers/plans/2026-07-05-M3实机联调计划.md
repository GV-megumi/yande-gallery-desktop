# M3 实机联调任务计划书

> **执行时机：全部里程碑（M4）完成后统一联调。** 本文档只做计划存档，联调时按节执行并填写结果记录。
> 依据：`android/README.md` §7 待实机验证清单、spec §8 错误处理与边界、§9 性能目标、M3 执行台账各任务「实机」记债项。

**目标：** 在真机 + 活桌面端实例上验证 M3 核心体验（配对/同步/大图三档/原图下载/搜索/多选/写操作/对账）的全部无头 CI 覆盖不到的真实语义，并按 spec §9 抽查性能目标。

**验收产出：** 本文档每个用例的「结果」列填写 ✅/❌/⚠️（❌ 须附复现步骤与日志/截图），汇总入 `doc/done/M3实机联调结果-<日期>.md`。

---

## 0. 环境与设备矩阵

### 0.1 桌面端准备

| # | 步骤 | 预期 |
|---|------|------|
| 0.1.1 | 桌面端「设置 → API 服务」：`enabled` 开、`mode` 切 **lan**，记下端口（默认 38947）与 Bearer Key | 服务监听局域网地址 |
| 0.1.2 | 权限键：开 `imageBinary`（缩略图/preview/原图二进制）、开 `imageWrite` + `galleryWrite`（写操作）、开 `eventsSubscribe`（SSE） | 四键全开（联调后半段会做「关闭权限」负例，先全开） |
| 0.1.3 | 测试图库内容：≥3 个图集、**>2000 张图（理想 ~1 万，否则 I.1/A.3 标记「按库规模折算」）**、若干带标签图（含中文标签）、至少 1 张大分辨率图（>4000px）与 1 张小图（<800px） | >2000 才能真实触发 sync 多页游标分页（limit 默认 2000）与大库进度展示（spec §8） |
| 0.1.4 | 手机与桌面同一局域网（同网段，无 AP 隔离） | ping 通 |

### 0.2 设备矩阵（按可得设备尽量覆盖）

| 档位 | API | 必测项 | 说明 |
|------|-----|--------|------|
| 主力机 | 33+（Android 13+） | 全部 A-J 节 | POST_NOTIFICATIONS 运行时提示属 M4 项（M3 无前台通知），见 §J |
| 中间档 | 29-32 | D 节（scoped storage IS_PENDING 路径）、**F.11/F.12（29 直删级联分支）**、G 节 | RELATIVE_PATH/IS_PENDING 分支；29 无 createDeleteRequest |
| legacy | 26-28（模拟器可） | D.5（MediaScanner 可见性）、WRITE_EXTERNAL_STORAGE 运行时授权、**F.11（直删级联分支）** | T7 审查项：`finalize` 经 DATA 列取真实路径触发扫描，Robolectric 无法验证 |
| 30+ | 30+ | **F.2/F.3（createDeleteRequest 系统确认弹窗）、F.12（重装所有权）** | 删除级联所有权语义 |

安装：`source .toolchain/env.sh && cd android && ./gradlew :app:assembleDebug`，adb install `app/build/outputs/apk/debug/app-debug.apk`（versionName 应为 M4 完成版本号）。

---

## A. 配对与首次同步

| # | 步骤 | 预期 | 结果 |
|---|------|------|------|
| A.1 | 首启 → 添加服务器 → 扫桌面端设置页二维码 | 相机权限弹窗 → 授权 → 配对成功即激活，自动开始首次全量同步（v0.2.0 修复项） | |
| A.2 | 删除服务器后改用手输 baseUrl + Key 配对 | 连接测试通过、保存即激活同步 | |
| A.3 | 首次全量同步进度 | 时间轴显示同步进度；1 万张 ≤10s（spec §9，按库规模折算记录实际值） | |
| A.4 | 同步完成后时间轴/相册 tab | 日期分组网格正确、图集卡片封面正确（含无封面兜底） | |
| A.5 | 缩略图加载 | 全部真实加载（`imageBinary` 已开）；滚动 60fps 观感、缓存命中即时显示（spec §9） | |
| A.6 | 下拉刷新 | 桌面端加/删图后，下拉刷新可见对应变化 | |
| A.7 | SSE 实时刷新 | 桌面端加图 ~2s 内安卓端自动出现（防抖对账）；长连挂 ≥30 分钟不被误杀（readTimeout=0 无心跳） | |

## B. 大图页（三档加载 + 手势）

| # | 步骤 | 预期 | 结果 |
|---|------|------|------|
| B.1 | 点网格第一张图进大图页 | 直接进入（无共享元素转场为 M3 已知形态，若 M4 已做则应有转场）；先显缩略档随即升 1600 档，无黑屏闪烁 | |
| B.2 | 左右滑动翻页 | 相邻图无感加载（预取已命中，spec §9）；首页（第 0 页）进入后立即左滑同样无感（02f5d34 回归项） | |
| B.3 | 双击 | 1x→2x→1x 循环，动画跟手 | |
| B.4 | 双指捏合至 5x | 钳制在 5x；缩放态下拖动平移；**横滑不翻页**（scale>1 门控）；缩回 1x 后横滑恢复翻页 | |
| B.5 | scale>2.5x 且未下载 | 出现「1600 档像素不足，可查看原图」轻提示（spec §7.3） | |
| B.6 | 单击 | 沉浸切换（系统栏隐/显）；有 ~300ms 双击判定延迟属已知形态 | |
| B.7 | scale==1 下滑 | 超阈值关闭返回网格；上滑不关闭 | |
| B.8 | 缩放后翻页再翻回 | 缩放已重置为适配（对齐小米相册行为） | |
| B.9 | 详情面板 | 文件名/分辨率/大小/格式/入库时间正确；标签 chips 可点跳搜索；所属图集可点跳图集详情 | |
| B.10 | 从图集详情进大图 | Pager 序与图集网格一致（galleryId 来源）；从时间轴进则与时间轴一致 | |

## C. 搜索

| # | 步骤 | 预期 | 结果 |
|---|------|------|------|
| C.1 | 照片 tab 顶栏搜索图标 → 搜索页 | 键盘自动弹出，空输入显示历史（首次为空态提示） | |
| C.2 | 输入标签前缀（含中文） | ~200ms 防抖后即时出结果；标签前缀命中 + 文件名包含命中 | |
| C.3 | 多关键词（空格分隔） | AND 交集——只显同时命中全部词的图 | |
| C.4 | IME 搜索键 | 写入历史；返回后再进搜索页历史可见、点击回填并搜索、清空可用 | |
| C.5 | 搜索结果点图 | 进大图页（时间轴序） | |
| C.6 | 输入 `%` 或 `_` | 不崩溃（已知限制：会过匹配，M4 若已做转义则应精确） | |

## D. 原图下载（MediaStore）

| # | 步骤 | 预期 | 结果 |
|---|------|------|------|
| D.1 | 大图页「查看原图」（29+ 主力机） | 状态流转：查看原图→下载中→已保存；完成后图片在系统相册 app 的 `Pictures/YandeGallery/` 可见 | |
| D.2 | 下载过程中打开系统相册 | **无半成品条目**（IS_PENDING=1 屏蔽） | |
| D.3 | 已下载图再进大图页 | 直读本地原图（跳 1600 档）；放大 5x 无像素不足提示 | |
| D.4 | 用系统相册手动删除该副本后回 app 看该图 | 回退 1600 档显示、downloads 映射自愈清理（spec §6.4）；「查看原图」恢复可点 | |
| D.5 | **API 26-28**：下载原图 | WRITE_EXTERNAL_STORAGE 运行时授权流程正常；完成后系统相册**真实可见**（T7 审查项：DATA 列取路径触发 MediaScanner——Robolectric 无法验证的核心项） | |
| D.6 | 断网中途 | 下载失败提示（WorkManager 重试属预期，恢复网络后自动续）；无半成品残留 | |
| D.7 | 桌面端删除某图后立即在安卓端「查看原图」 | 404 → 明确失败提示 + 触发 image-ids 对账，该图 ~2s 内从时间轴消失 | |
| D.8 | 存储空间不足（可选，模拟器填满磁盘） | 明确报错不静默（spec §8） | |

## E. 多选与批量

| # | 步骤 | 预期 | 结果 |
|---|------|------|------|
| E.1 | 时间轴长按一张图 | 进入多选并选中；角标出现；顶部计数/全选/取消栏出现（已知形态：与 AppScaffold 顶栏并存，M4 若已收敛则应单栏） | |
| E.2 | 点选/反选多张，跨日期分组 | 计数正确；日期头不可选 | |
| E.3 | 全选 | 选中全部**已加载**项（快照语义）；继续滚动加载后可再全选补齐 | |
| E.4 | 多选态按返回键 | 退出多选不退页 | |
| E.5 | 批量下载 | 逐个入队（重复入队去重）；全部完成后各图「已保存」 | |
| E.6 | 批量分享（全部已下载） | ACTION_SEND_MULTIPLE 到微信/相册等目标 app 可正常接收 | |
| E.7 | 批量分享（含未下载） | 提示「请先下载原图」（M3 简化版；M4 若做完整版则应自动下载后分享） | |
| E.8 | 批量删除 | 确认对话框明示数量与「本机副本不受影响」；确认后镜像即时消失、桌面端同步删除 | |
| E.9 | 批量加入图集 | 选择器不含（M4 修后）当前图集；加入后图集详情与桌面端一致 | |
| E.10 | 图集详情内多选 → 移出当前图集 | 移出后本图集不再显示、桌面端一致、图片本体保留 | |
| E.11 | 切换服务器后回来 | 多选已自动清空（05e68dc 守卫） | |

## F. 写操作（单图）与标签

| # | 步骤 | 预期 | 结果 |
|---|------|------|------|
| F.1 | 大图页删除（无本地副本） | 二次确认 → 镜像即时删除返回网格 → 桌面端该图消失（永久删除+双端） | |
| F.2 | 大图页删除（**有**本地副本，30+） | `createDeleteRequest` 系统确认弹窗出现；**同意** → 系统相册副本一并删除 | |
| F.3 | 同上，但系统弹窗**拒绝** | 仅清 downloads 映射；系统相册副本保留（spec §8 用户自主选择） | |
| F.4 | 详情面板加标签（桌面端已有的标签名） | chip 即时出现（乐观建链）；桌面端该图标签同步可见 | |
| F.5 | 加**新**标签名（桌面端不存在） | 提交成功；chip 在 ~2s 对账后出现（tag id 自愈，已知设计）；桌面端新标签已建 | |
| F.6 | 移除标签 | chip 即时消失；桌面端一致 | |
| F.7 | 相册 tab「+」新建图集 | 输入名（空名不可提交）→ 创建后卡片出现；桌面端可见空图集 | |
| F.8 | 卡片长按 → 重命名 | 对话框预填旧名 → 改名即时生效、桌面端一致 | |
| F.9 | 卡片长按 → 删除图集 | 确认文案说明不删图片文件 → 图集消失、成员图片仍在时间轴；桌面端一致 | |
| F.10 | 单图分享（已下载） | ACTION_SEND 目标 app 正常接收 | |
| F.11 | **API 26-29** 删除有本地副本的图 | `buildDeleteRequest` 返回 null 走直删分支：**无**系统弹窗、副本直接从系统相册消失、downloads 行清理 | |
| F.12 | **重装所有权**（spec §8）：下载副本 → 卸载重装 → 重新配对 → 删除该图（30+ 与 29 各测一台） | 30+：失去所有权 → createDeleteRequest 系统弹窗出现，同意删/拒绝保留；**API 29：当前实现会静默吞 RecoverableSecurityException（runCatching）——若副本残留且无任何提示，按实现缺陷上报**（联调计划 critic 预判项，M4 候选） | |
| F.13 | 桌面端先删某图 → 安卓端对同一图执行删除 | 写路径 404 视为成功：**静默成功无失败提示**、不回滚、触发对账（e2e 已覆盖，实机做一次确认） | |

## G. 对账与多服务器

| # | 步骤 | 预期 | 结果 |
|---|------|------|------|
| G.1 | 写操作后 2s 内观察 | SSE 事件触发对账（安卓侧无需手动刷新即与桌面端一致） | |
| G.2 | 配第二台桌面端（或同机第二实例/改端口），切换激活 | 自动触发新服务器同步；SSE 重连到新服务器（v0.2.0 修复项）；缩略图不串图（缓存键 serverId 隔离） | |
| G.3 | 服务器 A 下载过原图（记 imageId），切到 B（B 库含同号 id） | B 的同号图**不**显示 A 的本地文件、不显「已保存」（a5844c8 回归项：downloads 随 clearMirror 清空） | |
| G.4 | 桌面端恢复备份（dataVersion 变化） | 安卓端提示后全量重建镜像（spec §8） | |
| G.5 | 切回服务器 A | 重新全量同步（身份变化重建）；一切正常 | |

## H. 离线与权限负例

| # | 步骤 | 预期 | 结果 |
|---|------|------|------|
| H.1 | 断开 WiFi | 顶部横幅「未连接到 <服务器名>」；已缓存缩略图/1600 档可浏览；未缓存图占位+点按重试 | |
| H.2 | 离线时写按钮 | 删除/标签编辑/图集操作/多选写动作全部置灰不排队（spec §8）；下载/分享按各自逻辑提示 | |
| H.3 | 恢复 WiFi | 横幅消失、自动增量同步 | |
| H.4 | 桌面端换 Bearer Key（模拟 401） | 「密钥失效」提示引导重扫（写路径与读路径都汇入） | |
| H.5 | 桌面端关闭 `imageWrite` | 删图/标签编辑失败提示（403，按钮不隐藏）；开回后恢复 | |
| H.6 | 桌面端关闭 `galleryWrite` | 图集写操作同上 | |
| H.7 | 桌面端关闭 `imageBinary` | 缩略图/preview/原图全 403（README 已知典型现象），横幅/占位表现合理 | |
| H.8 | 桌面端改 IP（换网络） | 连接超时引导编辑地址/重扫（spec §8） | |

## I. 性能抽查（spec §9）

| # | 项 | 目标 | 实测 | 结果 |
|---|----|------|------|------|
| I.1 | 首次全量同步 | 1 万张 ≤10s（按实际库折算） | | |
| I.2 | 例行增量同步 | ≤1s | | |
| I.3 | 时间轴滚动 | 稳定 60fps 观感（gpu profiling 可选） | | |
| I.4 | 相邻图切换（已预取） | 无感；未预取 ≤500ms | | |
| I.5 | 捏合缩放 | 跟手无掉帧（derivedStateOf 收敛后回归观感） | | |

## J. M4 新增项（M4 收尾填充，v0.4.0；联调时执行）

> M4 已完成（版本 0.4.0/versionCode 4），以下为 M4 全部无头 CI 覆盖不到的实机验证项。
> K 节各已知限制的验收口径已按 M4 修复项同步更新——「已修」项按新行为验收。

| # | 步骤 | 预期 | 结果 |
|---|------|------|------|
| J.1 | **下载前台通知**（33+ 主力机首启 → 下载原图） | 首启进入主界面即弹 POST_NOTIFICATIONS 运行时请求（仅一次，旋转不重复弹）；授权后下载出前台通知、确定进度条随 written/total 推进、完成后通知消失；**拒绝授权** → 仅无进度通知，下载照常完成落库（静默降级，无阻断对话框） | |
| J.2 | **捏合切档手感**：时间轴双指捏合 MONTH(6列) ↔ DAY_5 ↔ DAY_4 ↔ DAY_3 全档往返 | 档位离散切换、格子 `animateItem` 位移/尺寸过渡帧率稳定；月↔日切换按原顶部日期锚定回位；杀进程重启回记忆档；**若全网格弹簧动画明显掉帧**，按 T3 注释退化「瞬时换列 + 整栏 100ms crossfade」再验 | |
| J.3 | **快速滚动滑块 + sticky 日期条**（>2000 张库） | 24dp 命中轨道拖动手感可用（thumb 仅 6dp 宽但整条轨道可拖）；拖动浮出日期气泡（月档「2026年6月」/日档「6月15日」，与 sticky 条同源同文案）；**拖到底**落到列表末尾（延展语义无死区）；松手落位准确；多选栏出现时滑块不被选择栏遮挡；sticky 顶部日期条随滚动实时更新 | |
| J.4 | **缓存管理页**（设置 → 缓存管理） | 缩略图/预览两档占用数字与磁盘实际对账（清理后归零）；清理后回时间轴缩略图重新拉取不残留坏图；调整上限滑块 → **重启后**生效（构建期定死，页内文案已注明）——重启后新上限对超额缓存逐步逐出 | |
| J.5 | **完整分享流**：单图未下载分享 + 批量混合（已下载/未下载/失败各含）分享 | 未下载项自动入队下载、终态后自动拉起分享面板（单图 ACTION_SEND / 批量 ACTION_SEND_MULTIPLE）；批量部分失败 → 成功项照常分享 + 失败计数提示；下载期间重复点分享不重复入队 | |
| J.6 | **转场观感（方案 B）**：网格点入大图、返回、跨图返回 | 进入 fade+scale（0.92→1，220ms）近似「放大展开」；返回反向收缩（160ms）；无黑屏闪烁、Pager 按 id 定位不受影响、黑色占位层时序正常。**可选增强（联调后定夺）：方案 A SharedTransitionLayout + 缩略图 hero 层**——集成点：NavHost 外包 `SharedTransitionLayout`、网格格子与 Viewer 当前页 `sharedElement(key = imageId)`、大图页黑色占位层（ViewerScreen 按 id 定位时序）改由 hero 接管（viewer-transition findings 方案 A 集成点清单照录）；若方案 B 观感已达标可不做 | |
| J.7 | **API 29 删除本地副本（含 F.12 重装所有权场景）** | 按 T9 新行为验收：29 设备失去所有权删除 → `RecoverableSecurityException` 转 NeedsConsent，拉起系统单条确认弹窗（IntentSender，与 30+ createDeleteRequest 语义对齐）；同意 → 副本删除；拒绝 → 副本保留、仅清 downloads 映射行（**不再静默吞异常**，F.12 预判缺陷已修） | |
| J.8 | **断网/恢复横幅即时性**（网络回调直驱） | 拔 WiFi：横幅「未连接到 <服务器名>」**即时**出现（系统网络回调直驱，不等下次同步失败）；恢复：横幅即时收起 + 自动一次增量同步 + SSE 重连（断网期间桌面端加的图 ~2s 内出现） | |
| J.9 | **离线未缓存占位快速失败 + 断网重启缓存直出** | 离线滚到未缓存图：占位**即时**出现（ACCESS_NETWORK_STATE 预判生效，非长超时等待）、点按重试可用、恢复网络后重试成功；**断网状态下杀进程重启**：已缓存缩略图/预览直出不发请求（M2-T9 respectCacheHeaders 真机确认，progress.md:67 记债闭环） | |
| J.10 | **changeSeq 升级路径**（桌面端 0.3.x → 含 changeSeq 版本） | 桌面升级迁移 bump dataVersion → 手机端下一次同步提示并触发**一次**全量重建；此后增量同步正常；桌面端同毫秒批量导入（脚本一次事务加多图）后增量同步**不漏行** | |

---

## K. 已知限制（M4 收尾更新验收口径：「已修」项**按新行为验收**，不再豁免）

来自 M3 各任务审查记债；M4 修复状态逐项标注——标「已修」的项联调时若仍复现**旧行为，按缺陷上报**：

1. ~~多选时 AppScaffold 顶栏与选择栏并存（双顶栏）~~ **已修（M4-T12 壳级收敛）**：照片 tab 多选态应为单一选择顶栏/底栏 swap（见 E.1）。
2. ~~大图页旋转屏幕回到初始打开图~~ **已修（M4-T13）**：旋转后应停留在当前图。
3. ~~分享为简化版~~ **已修（M4-T11 完整分享流）**：未下载项自动下载后分享，按 J.5 验收。
4. ~~无下载前台通知~~ **已修（M4-T10）**：确定进度前台通知 + 33+ 运行时权限，按 J.1 验收。
5. 单击沉浸有 ~300ms 双击判定延迟（手势方案固有）——**仍为已知形态**。
6. ~~搜索 `%`/`_` 过匹配~~ **已修（M4-T14 LIKE 转义）**：C.6 按精确匹配验收（输入 `%` 不再全命中）。
7. ~~空搜索页后台预载全表首页~~ **已修（M4-T14 空查询不建 Pager）**。
8. ~~删除确认文案无条件提及本地副本措辞~~ **已修（M4-T13 文案条件化）**：无本地副本时不再出现副本措辞。
9. ~~大图页 FAB 类写动作离线 TalkBack 仍宣告可点~~ **已修（M4-T13 FAB 离线语义）**：TalkBack 应宣告禁用。
10. ~~飞行中下载跨切服的窄竞态~~ **已修（M4-T9 downloads 表 serverId 化）**：G.3 按隔离语义验收。
11. ~~M1 sync 游标同毫秒边界漏同步~~ **已修（M4-T16 桌面端 changeSeq 单调游标）**：按 J.10 验收升级路径与同毫秒批量导入不漏行。

M4 自身的遗留定界（联调时**不作为缺陷**上报）：见 `android/README.md` §6「M4 已知后置项」——
方案 A 共享元素转场（可选增强，J.6）、捏合连续缩放（离散切档为设计定界，掉帧退化预案见 J.2）、
Paging placeholders 不启用、图集详情/搜索页无密度档与滑块、对账级联中所有权丢失副本仅清行保留文件、
缓存上限调整重启后生效（J.4）、桌面端 applyImagePage 未批量化。

## L. 结果记录模板

```markdown
# M3 实机联调结果-<日期>
- 设备：<型号/API>；桌面端：<版本/OS>；网络：<路由器型号/频段>
- 通过：X / 失败：Y / 跳过：Z（原因）
## 失败项明细
### <编号> <标题>
- 复现步骤：
- 预期 vs 实际：
- 日志/截图：
- 初判定级：Critical/Important/Minor
```

**退出标准：** A-I 节全部 ✅ 或失败项均已建修复票并复验通过；K 节确认无「已知限制之外」的新缺陷。
