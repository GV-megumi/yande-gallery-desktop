# 相册与文件夹解耦 — 阶段 1（数据层 / Expand）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在不破坏现有功能的前提下，引入 `gallery_folders` 与 `gallery_images` 两张关联表，并把现有相册的"文件夹绑定"和"图片归属"无损回填进去。

**Architecture:** 采用 expand/contract（并行变更）迁移。本阶段只**扩张**：建两张新表 + 幂等回填，`galleries` 旧列（`folderPath/isWatching/recursive/extensions`）原样保留，旧代码继续读写旧列，应用全程可用。后续阶段逐个把消费方切到新表；最末的 contract 阶段才重建 `galleries` 删旧列、把 `isWatching` 改名 `autoScan`。

**Tech Stack:** Electron 主进程 / TypeScript / sqlite3（node, 回调封装为 `run/get/all/columnExists`）/ Vitest（内存 SQLite）。

**关联规范：** 本仓库 commit message 用中文（类型前缀英文）；迁移风格为幂等命令式（`CREATE TABLE IF NOT EXISTS` + `columnExists` 守卫），见 `database.ts`。回填的 recursive 感知前缀匹配必须与 `galleryService.deleteGallery`（[galleryService.ts:309-323](../../../src/main/services/galleryService.ts#L309-L323)）**完全一致**，以保持图片归属与现状相同。

> **提交约定**：用户规则是"要我提交才提交"。计划中保留每个任务的 Commit 步骤作为方法论；执行时按用户确认进行（可逐个或合并提交）。

**本阶段对应 spec：** [2026-06-28-相册与文件夹解耦重构-design.md](../specs/2026-06-28-相册与文件夹解耦重构-design.md) 的 §4.2、§4.3、§11.1（步骤 1/3/4 的"扩张"部分）。后续阶段（服务原语、booru/app:// 关联、跨机器重定位、IPC/类型、渲染层、contract 重建）各自单独成计划。

---

## 文件结构

- **修改：** `src/main/services/database.ts`
  - 顶部新增 `import { normalizePath } from '../utils/path.js';`
  - 文件末尾新增并导出迁移函数：`ensureDecouplingTables` / `backfillGalleryFolders` / `backfillGalleryImages` / `migrateGalleryFolderDecoupling`
  - 在 `initDatabase()` 末尾（插入默认站点之前）调用 `migrateGalleryFolderDecoupling(database)`
- **新增测试：** `tests/main/services/galleryDecouplingMigration.test.ts`

> 迁移函数放进 `database.ts`（而非独立模块）有两个原因：① 与既有迁移风格一致（所有迁移都在 `database.ts`，CLAUDE.md 规定"schema 以 database.ts 为准"）；② 避免 `database.ts` ↔ 迁移模块的 ESM 循环依赖（迁移要用 `run/all/columnExists`）。

---

## Task 1：新增 `gallery_folders` / `gallery_images` 两张表

**Files:**
- Modify: `src/main/services/database.ts`（文件末尾追加导出函数；顶部加 import）
- Test: `tests/main/services/galleryDecouplingMigration.test.ts`

- [ ] **Step 1: 写失败测试**

创建 `tests/main/services/galleryDecouplingMigration.test.ts`：

```ts
import { describe, it, expect, beforeEach, afterEach } from 'vitest';
import sqlite3 from 'sqlite3';
import path from 'path';
import { run, get, all, columnExists } from '../../../src/main/services/database';
import { normalizePath } from '../../../src/main/utils/path';
import {
  migrateGalleryFolderDecoupling,
  ensureDecouplingTables,
  backfillGalleryFolders,
  backfillGalleryImages,
} from '../../../src/main/services/database';

let db: sqlite3.Database;

/** 建迁移前的旧结构（galleries 含 folderPath；images 全局唯一 filepath） */
async function setupLegacySchema(): Promise<void> {
  await run(db, `
    CREATE TABLE images (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      filename TEXT NOT NULL,
      filepath TEXT NOT NULL UNIQUE,
      fileSize INTEGER NOT NULL,
      width INTEGER NOT NULL,
      height INTEGER NOT NULL,
      format TEXT NOT NULL,
      createdAt TEXT NOT NULL,
      updatedAt TEXT NOT NULL
    )
  `);
  await run(db, `
    CREATE TABLE galleries (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      folderPath TEXT NOT NULL UNIQUE,
      name TEXT NOT NULL,
      coverImageId INTEGER,
      imageCount INTEGER DEFAULT 0,
      lastScannedAt TEXT,
      isWatching INTEGER DEFAULT 1,
      recursive INTEGER DEFAULT 1,
      extensions TEXT,
      createdAt TEXT NOT NULL,
      updatedAt TEXT NOT NULL
    )
  `);
}

async function addImage(filepath: string): Promise<number> {
  await run(
    db,
    `INSERT INTO images (filename, filepath, fileSize, width, height, format, createdAt, updatedAt)
     VALUES (?, ?, 0, 0, 0, 'jpg', '2024-01-01', '2024-01-01')`,
    [path.basename(filepath), filepath]
  );
  const row = await get<{ id: number }>(db, 'SELECT last_insert_rowid() as id');
  return row!.id;
}

async function addGallery(folderPath: string, name: string, recursive: number, isWatching = 1): Promise<number> {
  await run(
    db,
    `INSERT INTO galleries (folderPath, name, isWatching, recursive, extensions, createdAt, updatedAt)
     VALUES (?, ?, ?, ?, ?, '2024-01-01', '2024-01-01')`,
    [folderPath, name, isWatching, recursive, JSON.stringify(['.jpg'])]
  );
  const row = await get<{ id: number }>(db, 'SELECT last_insert_rowid() as id');
  return row!.id;
}

beforeEach(async () => {
  db = await new Promise<sqlite3.Database>((resolve, reject) => {
    const database = new sqlite3.Database(':memory:', (err) => (err ? reject(err) : resolve(database)));
  });
  await run(db, 'PRAGMA foreign_keys=ON');
  await setupLegacySchema();
});

afterEach(async () => {
  await new Promise<void>((resolve, reject) => db.close((err) => (err ? reject(err) : resolve())));
});

describe('ensureDecouplingTables', () => {
  it('建出 gallery_folders / gallery_images 两张表且列正确', async () => {
    await ensureDecouplingTables(db);

    expect(await columnExists(db, 'gallery_folders', 'galleryId')).toBe(true);
    expect(await columnExists(db, 'gallery_folders', 'folderPath')).toBe(true);
    expect(await columnExists(db, 'gallery_folders', 'recursive')).toBe(true);
    expect(await columnExists(db, 'gallery_folders', 'extensions')).toBe(true);

    expect(await columnExists(db, 'gallery_images', 'galleryId')).toBe(true);
    expect(await columnExists(db, 'gallery_images', 'imageId')).toBe(true);
    expect(await columnExists(db, 'gallery_images', 'addedAt')).toBe(true);
  });

  it('幂等：重复建表不报错', async () => {
    await ensureDecouplingTables(db);
    await expect(ensureDecouplingTables(db)).resolves.toBeUndefined();
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npx vitest run tests/main/services/galleryDecouplingMigration.test.ts`
Expected: FAIL —— 导入 `ensureDecouplingTables`（以及其它三个）从 `database` 不存在，报 `does not provide an export named 'ensureDecouplingTables'` 或相关错误。

- [ ] **Step 3: 实现 `ensureDecouplingTables`**

在 `src/main/services/database.ts` 顶部 import 区（第 4 行 `import { getDatabasePath } ...` 之后）追加：

```ts
import { normalizePath } from '../utils/path.js';
```

在 `database.ts` **文件末尾**（`isDatabaseInitialized` 函数之后）追加：

```ts
// ===========================================================================
// 相册与文件夹解耦迁移（Expand 阶段）
//
// 引入两张关联表，把"图片归属"从 filepath 前缀隐式匹配升级为显式成员表：
//   - gallery_folders：相册 ↔ 文件夹（1:N），folderPath 全局唯一
//   - gallery_images ：相册 ↔ 图片成员（主键 galleryId+imageId）
//
// 本迁移只"扩张"（建表 + 回填），不动 galleries 旧列（folderPath/isWatching/
// recursive/extensions），保证迁移期间旧代码仍可运行。旧列清理（galleries 重建、
// isWatching→autoScan 改名）留到所有消费方改造完成后的 contract 阶段。
//
// 幂等：建表用 IF NOT EXISTS；回填用 INSERT OR IGNORE（folderPath 唯一 / 成员主键）。
// ===========================================================================

/** 建 gallery_folders / gallery_images 两张表与索引（幂等） */
export async function ensureDecouplingTables(database: sqlite3.Database): Promise<void> {
  await run(database, `
    CREATE TABLE IF NOT EXISTS gallery_folders (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      galleryId INTEGER NOT NULL,
      folderPath TEXT NOT NULL UNIQUE,   -- 全局唯一：一个文件夹只属于一个相册
      recursive INTEGER NOT NULL DEFAULT 1,
      extensions TEXT,                   -- JSON 数组；为空时由调用方用默认扩展名
      createdAt TEXT NOT NULL,
      updatedAt TEXT NOT NULL,
      FOREIGN KEY (galleryId) REFERENCES galleries (id) ON DELETE CASCADE
    )
  `);

  await run(database, `
    CREATE TABLE IF NOT EXISTS gallery_images (
      galleryId INTEGER NOT NULL,
      imageId INTEGER NOT NULL,
      addedAt TEXT NOT NULL,
      PRIMARY KEY (galleryId, imageId),
      FOREIGN KEY (galleryId) REFERENCES galleries (id) ON DELETE CASCADE,
      FOREIGN KEY (imageId) REFERENCES images (id) ON DELETE CASCADE
    )
  `);

  await run(database, `CREATE INDEX IF NOT EXISTS idx_gallery_folders_galleryId ON gallery_folders (galleryId)`);
  await run(database, `CREATE INDEX IF NOT EXISTS idx_gallery_images_imageId ON gallery_images (imageId)`);
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run tests/main/services/galleryDecouplingMigration.test.ts`
Expected: PASS（`ensureDecouplingTables` 的 2 个用例通过；其余三个导入虽存在但尚无用例引用）。

- [ ] **Step 5: 提交**

```bash
git add src/main/services/database.ts tests/main/services/galleryDecouplingMigration.test.ts
git commit -m "feat: 新增 gallery_folders/gallery_images 关联表（解耦迁移 Expand）"
```

---

## Task 2：回填 `gallery_folders`（每个旧相册一条文件夹绑定）

**Files:**
- Modify: `src/main/services/database.ts`
- Test: `tests/main/services/galleryDecouplingMigration.test.ts`

- [ ] **Step 1: 写失败测试**

在测试文件末尾追加：

```ts
describe('backfillGalleryFolders', () => {
  it('每个相册回填一条绑定，含 folderPath/recursive/extensions', async () => {
    const galA = normalizePath(path.join('M:', 'galA'));
    const galB = normalizePath(path.join('M:', 'galB'));
    const aId = await addGallery(galA, 'galA', 1);
    const bId = await addGallery(galB, 'galB', 0);

    await ensureDecouplingTables(db);
    await backfillGalleryFolders(db);

    const rows = await all<{ galleryId: number; folderPath: string; recursive: number; extensions: string }>(
      db,
      'SELECT galleryId, folderPath, recursive, extensions FROM gallery_folders ORDER BY galleryId'
    );
    expect(rows).toHaveLength(2);
    expect(rows[0]).toMatchObject({ galleryId: aId, folderPath: galA, recursive: 1 });
    expect(rows[1]).toMatchObject({ galleryId: bId, folderPath: galB, recursive: 0 });
    expect(JSON.parse(rows[0].extensions)).toEqual(['.jpg']);
  });

  it('幂等：重复回填不产生重复绑定', async () => {
    const galA = normalizePath(path.join('M:', 'galA'));
    await addGallery(galA, 'galA', 1);
    await ensureDecouplingTables(db);
    await backfillGalleryFolders(db);
    await backfillGalleryFolders(db);
    const rows = await all(db, 'SELECT * FROM gallery_folders');
    expect(rows).toHaveLength(1);
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npx vitest run tests/main/services/galleryDecouplingMigration.test.ts -t backfillGalleryFolders`
Expected: FAIL —— `backfillGalleryFolders` 尚未实现（运行时 `backfillGalleryFolders is not a function` / 导入为 undefined）。

- [ ] **Step 3: 实现 `backfillGalleryFolders`**

在 `database.ts` 的 `ensureDecouplingTables` 之后追加：

```ts
/**
 * 从旧 galleries.folderPath 回填 gallery_folders（每个相册一条绑定）。
 * 幂等：folderPath 全局唯一 + INSERT OR IGNORE。
 * 仅在 galleries 仍含 folderPath 列时由编排函数调用。
 */
export async function backfillGalleryFolders(database: sqlite3.Database): Promise<void> {
  await run(database, `
    INSERT OR IGNORE INTO gallery_folders (galleryId, folderPath, recursive, extensions, createdAt, updatedAt)
    SELECT id, folderPath, COALESCE(recursive, 1), extensions, createdAt, updatedAt
      FROM galleries
     WHERE folderPath IS NOT NULL AND folderPath <> ''
  `);
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run tests/main/services/galleryDecouplingMigration.test.ts -t backfillGalleryFolders`
Expected: PASS（2 个用例）。

- [ ] **Step 5: 提交**

```bash
git add src/main/services/database.ts tests/main/services/galleryDecouplingMigration.test.ts
git commit -m "feat: 回填 gallery_folders（从旧 galleries.folderPath）"
```

---

## Task 3：回填 `gallery_images`（recursive 感知，复刻 deleteGallery 匹配）

**Files:**
- Modify: `src/main/services/database.ts`
- Test: `tests/main/services/galleryDecouplingMigration.test.ts`

- [ ] **Step 1: 写失败测试**

在测试文件末尾追加（核心正确性用例：递归相册含嵌套、非递归相册仅含直接图片）：

```ts
describe('backfillGalleryImages', () => {
  it('递归相册含嵌套图片，非递归相册仅含直接图片', async () => {
    const galA = normalizePath(path.join('M:', 'galA')); // recursive=1
    const galB = normalizePath(path.join('M:', 'galB')); // recursive=0
    const aId = await addGallery(galA, 'galA', 1);
    const bId = await addGallery(galB, 'galB', 0);

    const aDirect = await addImage(normalizePath(path.join('M:', 'galA', 'a.jpg')));
    const aNested = await addImage(normalizePath(path.join('M:', 'galA', 'sub', 'b.jpg')));
    const bDirect = await addImage(normalizePath(path.join('M:', 'galB', 'c.jpg')));
    const bNested = await addImage(normalizePath(path.join('M:', 'galB', 'sub', 'd.jpg')));

    await ensureDecouplingTables(db);
    await backfillGalleryImages(db);

    const aMembers = (
      await all<{ imageId: number }>(db, 'SELECT imageId FROM gallery_images WHERE galleryId = ? ORDER BY imageId', [aId])
    ).map((r) => r.imageId);
    const bMembers = (
      await all<{ imageId: number }>(db, 'SELECT imageId FROM gallery_images WHERE galleryId = ? ORDER BY imageId', [bId])
    ).map((r) => r.imageId);

    expect(aMembers).toEqual([aDirect, aNested].sort((x, y) => x - y)); // 递归：直接 + 嵌套
    expect(bMembers).toEqual([bDirect]);                                // 非递归：仅直接
    // 反向确认：嵌套图片没有被错误塞进非递归相册
    expect(bMembers).not.toContain(bNested);
  });

  it('幂等：重复回填不产生重复成员', async () => {
    const galA = normalizePath(path.join('M:', 'galA'));
    const aId = await addGallery(galA, 'galA', 1);
    await addImage(normalizePath(path.join('M:', 'galA', 'a.jpg')));
    await ensureDecouplingTables(db);
    await backfillGalleryImages(db);
    await backfillGalleryImages(db);
    const members = await all(db, 'SELECT * FROM gallery_images WHERE galleryId = ?', [aId]);
    expect(members).toHaveLength(1);
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npx vitest run tests/main/services/galleryDecouplingMigration.test.ts -t backfillGalleryImages`
Expected: FAIL —— `backfillGalleryImages` 尚未实现。

- [ ] **Step 3: 实现 `backfillGalleryImages`**

在 `database.ts` 的 `backfillGalleryFolders` 之后追加（匹配逻辑与 `galleryService.deleteGallery` 完全一致）：

```ts
/**
 * 回填 gallery_images：对每个相册，按与 deleteGallery 一致的 recursive 感知前缀匹配
 * 选出其图片写入成员表。幂等（成员主键 + INSERT OR IGNORE）。
 *
 * 匹配规则（与 galleryService.deleteGallery 一致，保持图片归属与现状相同）：
 *   - recursive=1：filepath LIKE 'F{sep}%' OR filepath = 'F'
 *   - recursive=0：filepath LIKE 'F{sep}%' AND filepath NOT LIKE 'F{sep}%{sep}%'
 * 其中 F = normalizePath(folderPath)，sep = path.sep（与入库 filepath 分隔符一致）。
 */
export async function backfillGalleryImages(database: sqlite3.Database): Promise<void> {
  const galleries = await all<{ id: number; folderPath: string; recursive: number }>(
    database,
    `SELECT id, folderPath, recursive FROM galleries WHERE folderPath IS NOT NULL AND folderPath <> ''`
  );

  const now = new Date().toISOString();

  for (const g of galleries) {
    const normalized = normalizePath(g.folderPath);
    const likePrefix = normalized + path.sep;
    const isRecursive = g.recursive === 1 || (g.recursive as unknown as boolean) === true;

    const images = isRecursive
      ? await all<{ id: number }>(
          database,
          `SELECT id FROM images WHERE filepath LIKE ? OR filepath = ?`,
          [likePrefix + '%', normalized]
        )
      : await all<{ id: number }>(
          database,
          `SELECT id FROM images WHERE filepath LIKE ? AND filepath NOT LIKE ?`,
          [likePrefix + '%', likePrefix + '%' + path.sep + '%']
        );

    for (const img of images) {
      await run(
        database,
        `INSERT OR IGNORE INTO gallery_images (galleryId, imageId, addedAt) VALUES (?, ?, ?)`,
        [g.id, img.id, now]
      );
    }
  }
}
```

> 性能说明：逐图片 `INSERT OR IGNORE` 对一次性迁移足够（与 `database.ts` 现有迁移同风格，无显式事务；幂等保证中断后重启可续）。若实测大库偏慢，后续可改分批多值 INSERT，不影响语义。

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run tests/main/services/galleryDecouplingMigration.test.ts -t backfillGalleryImages`
Expected: PASS（2 个用例）。

- [ ] **Step 5: 提交**

```bash
git add src/main/services/database.ts tests/main/services/galleryDecouplingMigration.test.ts
git commit -m "feat: 回填 gallery_images（recursive 感知，复刻 deleteGallery 匹配）"
```

---

## Task 4：编排函数 `migrateGalleryFolderDecoupling`（幂等 + contract 后跳过）

**Files:**
- Modify: `src/main/services/database.ts`
- Test: `tests/main/services/galleryDecouplingMigration.test.ts`

- [ ] **Step 1: 写失败测试**

在测试文件末尾追加：

```ts
describe('migrateGalleryFolderDecoupling', () => {
  it('完整迁移：建表 + 回填 folders + 回填 images', async () => {
    const galA = normalizePath(path.join('M:', 'galA'));
    const aId = await addGallery(galA, 'galA', 1);
    const aImg = await addImage(normalizePath(path.join('M:', 'galA', 'a.jpg')));

    await migrateGalleryFolderDecoupling(db);

    const folders = await all(db, 'SELECT * FROM gallery_folders WHERE galleryId = ?', [aId]);
    const members = await all<{ imageId: number }>(db, 'SELECT imageId FROM gallery_images WHERE galleryId = ?', [aId]);
    expect(folders).toHaveLength(1);
    expect(members.map((m) => m.imageId)).toEqual([aImg]);
  });

  it('幂等：连续两次迁移结果不变', async () => {
    const galA = normalizePath(path.join('M:', 'galA'));
    await addGallery(galA, 'galA', 1);
    await addImage(normalizePath(path.join('M:', 'galA', 'a.jpg')));

    await migrateGalleryFolderDecoupling(db);
    await migrateGalleryFolderDecoupling(db);

    expect(await all(db, 'SELECT * FROM gallery_folders')).toHaveLength(1);
    expect(await all(db, 'SELECT * FROM gallery_images')).toHaveLength(1);
  });

  it('contract 后（galleries 无 folderPath 列）：只建表、不回填、不报错', async () => {
    await run(db, 'DROP TABLE galleries');
    await run(db, `
      CREATE TABLE galleries (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        name TEXT NOT NULL,
        autoScan INTEGER NOT NULL DEFAULT 1,
        createdAt TEXT NOT NULL,
        updatedAt TEXT NOT NULL
      )
    `);

    await expect(migrateGalleryFolderDecoupling(db)).resolves.toBeUndefined();
    expect(await columnExists(db, 'gallery_folders', 'galleryId')).toBe(true);
    expect(await all(db, 'SELECT * FROM gallery_images')).toEqual([]);
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npx vitest run tests/main/services/galleryDecouplingMigration.test.ts -t migrateGalleryFolderDecoupling`
Expected: FAIL —— `migrateGalleryFolderDecoupling` 尚未实现。

- [ ] **Step 3: 实现 `migrateGalleryFolderDecoupling`**

在 `database.ts` 的 `backfillGalleryImages` 之后追加：

```ts
/**
 * 解耦迁移编排（幂等）。在 initDatabase 末尾调用。
 * - 先建两张关联表（始终执行）。
 * - 仅当 galleries 仍是旧结构（含 folderPath 列）时回填 folders/images；
 *   contract 阶段删列后该判断为 false，回填自动跳过。
 */
export async function migrateGalleryFolderDecoupling(database: sqlite3.Database): Promise<void> {
  await ensureDecouplingTables(database);

  if (await columnExists(database, 'galleries', 'folderPath')) {
    await backfillGalleryFolders(database);
    await backfillGalleryImages(database);
    console.log('[database] 相册解耦迁移：gallery_folders / gallery_images 回填完成');
  }
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run tests/main/services/galleryDecouplingMigration.test.ts`
Expected: PASS（全文件用例，含编排 3 个 + 前述 6 个）。

- [ ] **Step 5: 提交**

```bash
git add src/main/services/database.ts tests/main/services/galleryDecouplingMigration.test.ts
git commit -m "feat: 解耦迁移编排函数（幂等，contract 后自动跳过回填）"
```

---

## Task 5：接入 `initDatabase`，全量校验

**Files:**
- Modify: `src/main/services/database.ts:722-724`（`initDatabase` 内，"插入默认站点"之前）

- [ ] **Step 1: 在 `initDatabase` 末尾调用编排函数**

在 `database.ts` 的 `initDatabase()` 中，定位 booru_favorites 迁移 `try/catch` 结束（第 722 行 `}` 之后）、`// 插入默认站点（如果不存在）`（第 724 行）**之前**，插入：

```ts
    // === 相册与文件夹解耦迁移（Expand：建关联表 + 回填，不动旧列） ===
    console.log('[database] 开始相册与文件夹解耦迁移...');
    await migrateGalleryFolderDecoupling(database);
    console.log('[database] 相册与文件夹解耦迁移完成');

```

- [ ] **Step 2: 类型检查**

Run: `npm run typecheck`
Expected: PASS（0 错误）。

- [ ] **Step 3: 运行解耦迁移测试 + 既有相册相关测试，确认无回归**

Run: `npx vitest run tests/main/services/galleryDecouplingMigration.test.ts tests/main/services/database.test.ts tests/main/services/galleryService.deleteGallery.test.ts tests/main/services/galleryService.test.ts`
Expected: PASS（新文件全绿；既有数据库/相册测试不受影响）。

- [ ] **Step 4: 运行全量测试**

Run: `npx vitest run`
Expected: PASS（全部文件通过；本阶段只新增表与回填，不改动任何现有读写路径，应无回归）。

- [ ] **Step 5: 提交**

```bash
git add src/main/services/database.ts
git commit -m "feat: initDatabase 接入相册解耦迁移（Expand 阶段完成）"
```

---

## 自检（Self-Review）

**1. Spec 覆盖：**
- §4.2 `gallery_folders` 表 → Task 1 建表（列/唯一/FK 一致）✔
- §4.3 `gallery_images` 表 → Task 1 建表（复合主键 + imageId 索引）✔
- §11.1 步骤 1（建两张新表）→ Task 1 ✔
- §11.1 步骤 3（回填 gallery_folders）→ Task 2 ✔
- §11.1 步骤 4（按 recursive 感知前缀回填 gallery_images）→ Task 3 ✔
- §11.1 步骤 2（galleries 重建、autoScan）→ **本阶段不做**，已明确移交 contract 阶段（expand/contract）。spec §11.1 步骤 2/5 的"重建/幂等判据"在 contract 计划里落地。
- §11.1 步骤 5（幂等）→ Task 2/3/4 均有幂等用例 ✔

**2. 占位符扫描：** 无 TBD/TODO；每个 Step 含完整代码与可运行命令、明确预期。✔

**3. 类型/命名一致性：**
- 四个导出函数名 `ensureDecouplingTables / backfillGalleryFolders / backfillGalleryImages / migrateGalleryFolderDecoupling` 在测试导入、实现、接入处一致。✔
- 参数名统一为 `database`（与 `database.ts` 内既有 helper 调用风格一致）。✔
- 回填匹配 SQL 与 `deleteGallery`（[galleryService.ts:309-323](../../../src/main/services/galleryService.ts#L309-L323)）逐字一致（`likePrefix = normalized + path.sep`，recursive 两分支）。✔

**4. 范围：** 单一阶段（数据层 Expand），可独立测试、可独立提交，不破坏现有功能。✔

> 已知保留行为：前缀 LIKE 未对 `_` / `%` 转义（如文件夹名含 `_`，理论上 `_` 在 LIKE 里是单字符通配）。这是 `deleteGallery` 现有行为；回填刻意**复刻**它以保证迁移后归属与现状一致。是否收紧转义属于独立改进，不在本阶段。

---

## 执行交接

**计划完成，保存在 `docs/superpowers/plans/2026-06-28-相册与文件夹解耦-阶段1-数据层.md`。两种执行方式：**

**1. Subagent-Driven（推荐）** —— 每个 Task 派一个全新 subagent，任务间我来评审，迭代快。

**2. Inline Execution** —— 在当前会话用 executing-plans 跑，批量执行 + 检查点评审。

**选哪种？**（另：本阶段是多阶段重构的第 1 阶段；执行完它再写"阶段 2：服务层原语"计划。）
