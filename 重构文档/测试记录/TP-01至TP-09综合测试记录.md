# TP-01 ~ TP-09 综合测试记录

## 1. 记录目的

本记录对 `重构文档/测试用例/TP-01.md` 至 `TP-09.md` 的执行状态做统一交付说明。
这些 TP 的实现已经沉淀在 `feat/refactor-todo-full` 分支上（959353b 检查点
及后续 fix/feat 切片），但单独的执行记录文档之前缺失。本文档补齐这部分证
据，列明：

- 每个 TP 已完成的闭环与覆盖测试
- 每个 TP 仍然待补测或后续切片继续的部分
- 与本轮“不合并分支，推送到 origin”的交付边界保持一致

本记录**不扩大**为“所有 TP 全部完成”的结论。对仍留在待补测范畴的部分，文档
会显式点名，留给后续切片执行。

## 2. 执行环境

- 执行时间：2026-04-16
- 工作目录：`M:\yande\yande-gallery-desktop\.worktrees\refactor-todo-full`
- 分支：`feat/refactor-todo-full`
- 基线：`e5e9e09 chore: 忽略本地 worktree 目录`
- 提交链：
  - `959353b refactor: TP-01~TP-13 重构整改检查点`
  - `c442521 fix(TP-07): 备份导出/恢复时剔除 booru_sites 的 salt/apiKey/passwordHash`
  - `123b76f chore: 提高 vitest 默认 testTimeout 到 30s 减少并发测试环境抖动误报`
  - `c15ab7a fix(TP-04): 移除未生效的 autoGenThumbnail 本地化字符串`
  - `4bf772c fix: 修复 tsc -p tsconfig.main.json 报错的若干类型问题`
  - `8cf5c0c feat(TP-03): 单图下载完成前校验 Booru 提供的 md5 指纹`
- 平台：Windows 10 Home China 10.0.19045
- Shell：bash
- 主要验证命令：
  ```bash
  npx vitest run
  npm run build
  ```
- 最终自动化结果摘要：
  - `vitest run`：98/98 测试文件、1666/1666 测试通过
  - `npm run build`：main/preload/renderer 三段 tsc+vite 均构建成功

## 3. 独立审查轮次

为满足 TODO.md 中“至少三次 review 且派发独立智能体”的要求，除在线编码自审
外，另派发了 3 轮独立审查智能体进行交叉复核：

1. **第 1 轮**：TP-07 `config:changed` 摘要广播切片专项复核（结论：无敏感字段
   泄漏、订阅兼容、类型一致）。
2. **第 2 轮**：架构 + 安全综合复核（IPC 单源、app:// 收容、openExternal、
   webview 安全、敏感字段出口、DTO 归一化、consolidation 残留）。
   结论：全部 PASS，仅 `src/main/ipc/channels.js` 残留为 hygiene 提醒（已
   评估保留以兼容 vitest 模块解析）。
3. **第 3 轮**：功能 + 回归综合复核（TP-01/02/03/04/09/11/13 实装状态与回归
   面，尤其是 pause vs failed、原子领取、md5 校验、pseudo-ability 闭环、
   hook 下沉面等）。
   结论：P0 均已收敛；TP-04 伪能力按“要么落地要么移除”原则处理；TP-03
   md5 空洞在 8cf5c0c 后闭合；TP-11 为分切片下沉，仍有后续切片余量。

## 4. 各 TP 当前状态

### TP-01 下载状态机与暂停/恢复/取消语义

- 状态：**主要完成**
- 证据：
  - `src/main/services/downloadManager.ts` 中 `pauseDownload` / `pauseAll`
    通过 `userInterruptedStatuses` 标记把用户主动暂停与真实失败分离，
    不再被 error 兜底污染为 `failed`。
  - 对应自动化：`tests/main/services/downloadManager.test.ts`、
    `tests/main/services/downloadManager.state.test.ts`。
- 已知待补测：
  - 单任务 `cancel` 能力：当前 `downloadManager` 并未导出 `cancelDownload`
    方法，UI 层也不存在单条取消按钮；P1-19 已不再表现为 UI 伪能力，但仍
    属“未实装”的纯功能缺口，归入后续切片。

### TP-02 批量调度生命周期与原子领取

- 状态：**完成**
- 证据：
  - `activeDownloadSessions` 在 `downloadLoop` 的 finally 中才清理，
    生命周期覆盖整个循环。
  - `claimPendingRecords` 通过
    `UPDATE bulk_download_records SET status = ? WHERE status = 'pending' AND url = ? AND sessionId = ?`
    + `runWithChanges` 原子领取；`bulk_download_records` 表 PRIMARY KEY
    为 (url, sessionId)。
  - 自动化：`tests/main/services/bulkDownloadService.tp02.test.ts`、
    `tests/main/services/bulkDownloadService.abort.test.ts`。

### TP-03 下载文件协议与完整性校验

- 状态：**单图已完成，批量部分收口**
- 证据：
  - `src/main/services/downloadFileProtocol.ts` 提供
    `.part` 临时文件协议、`replaceFileWithTemp` 原子替换、`.backup`
    回滚、`validateDownloadedFileSize` 大小校验。
  - 8cf5c0c 新增 `validateDownloadedFileMd5`，并在 `downloadManager`
    单图下载路径中在 size 校验通过后调用；不一致时删除 `.part`
    并抛错走统一失败分支。
  - 自动化：`tests/main/services/downloadFileProtocol.test.ts` 12 条
    全部 GREEN。
- 待补测：
  - 批量下载 (bulkDownloadService) 尚未接入 md5 校验：现有
    `BulkDownloadRecord` 表结构不携带 md5，后续切片需先补 schema
    迁移并回填。

### TP-04 页面伪能力与功能闭环

- 状态：**P0/P1 闭环已处理，余项按“移除”原则关闭**
- 证据：
  - `autoGenThumbnail` 在代码和 UI 中均无消费者，c15ab7a 已移除残留
    i18n 字符串（P1-18）。
  - `pageMode` 当前不在任何 Settings UI 中暴露给用户，仅作为
    `BooruAppearancePreference` 类型字段保留，默认值 `'pagination'`；
    不构成用户可见的伪能力（P1-17 降级为配置留位）。
  - 单图“取消下载”按钮在当前 UI 中已不是 UI 伪能力（BooruDownloadPage
    仅提供暂停 + Popconfirm，没有 TODO 按钮）；对应“单任务 cancel 能力
    实装”归入 TP-01 后续切片（见上文）。
  - FavoriteTags 下载路径闭环、SavedSearch 编辑态、InvalidImages 空/失败
    态分离、Gallery 删除闭环等均已在 959353b 检查点前完成，并对应
    `tests/renderer/pages/*` 下的若干自动化测试。

### TP-05 统一 IPC 协议单一来源

- 状态：**完成**
- 证据：
  - `src/main/ipc/handlers-full.ts` 已删除。
  - `src/preload/index.ts` 与 `src/main/ipc/handlers.ts` 中不再出现
    以裸字符串形式调用的 IPC 通道，全部复用
    `src/main/ipc/channels.ts` 的 `IPC_CHANNELS` 常量。
  - 自动化：`tests/main/ipc/setupIPC.test.ts` 包含针对
    preload/handlers/channels 单一来源的源码级断言。
- 说明：
  - `src/main/ipc/channels.js` 暂予保留，原因在于 vitest 的 `vi.mock`
    解析行为依赖该 `.js` 入口，删除后会触发 hoisting/解析失败；
    独立审查轮次 2 已评估为“保留 > 删除”。后续若改造 vitest
    解析配置，可再行清理。

### TP-06 收缩 preload / shared / handler 边界

- 状态：**完成**
- 证据：
  - Preload 不再暴露敏感站点字段（salt / apiKey / passwordHash）。
  - BooruSite DTO 通过 `toRendererSafeBooruSite` 输出受控形态。
  - Wiki / Forum / Profile IPC 返回 shared 层定义的 camelCase DTO
    而非原始 upstream 字段。
  - 自动化：`tests/main/ipc/setupIPC.test.ts` 的 "Wiki/Forum/Profile IPC 应返回
    shared 层定义的 camelCase DTO" 等用例；
    `tests/renderer/pages/BooruBoundaryPages.test.tsx` 对应渲染端契约。

### TP-07 收紧配置凭证与外部能力安全边界

- 状态：**完成**
- 证据：
  - Window / webview 安全：`setWindowOpenHandler`、`will-navigate`、
    `will-attach-webview` 强制 `nodeIntegration=false`、`contextIsolation=true`、
    `sandbox=true`，并维护 webview 源白名单。
  - `system:open-external` 校验：拒绝非 https、带凭据、内网/回环、
    DNS 解析到私网的目标；自动化在
    `tests/main/ipc/externalLinkSecurity.test.ts`。
  - `app://` 协议收容：`resolveAppProtocolFilePath` 以 `realpathSync`
    校验受控根目录，拒绝越界、越盘、软链接逃逸；自动化在
    `tests/main/index.appProtocol.test.ts`。
  - 敏感配置最小暴露：`toRendererSafeConfig` 去掉 proxy
    username/password 与 google clientSecret；`config:changed`
    事件只下发 `ConfigChangedSummary`（`{ version, sections }`）摘要，
    preload 收到后按需重新调用 `CONFIG_GET` / `BOORU_PREFERENCES_GET_APPEARANCE`
    拉取最新去敏 DTO。
  - 备份导出/恢复：配置部分通过 `projectBackupSafeConfig` 做白名单重建；
    `booru_sites` 表列通过 `SENSITIVE_COLUMNS_BY_TABLE` 在导出与导入时
    都剔除 salt / apiKey / passwordHash。
  - 自动化：`tests/main/services/backupService.test.ts`（13 条）、
    `tests/main/ipc/setupIPC.test.ts`（27 条）、
    `tests/main/window.security.test.ts`、
    `tests/main/ipc/externalLinkSecurity.test.ts`、
    `tests/main/index.appProtocol.test.ts` 均 GREEN。

### TP-08 运行入口启动时序与退出治理

- 状态：**主要完成**
- 证据：
  - 新增 `tests/main/index.startup.test.ts`、`tests/main/index.lifecycle.test.ts`、
    `tests/main/index.tray.test.ts` 覆盖启动/生命周期/托盘主链路。
  - `before-quit`、`will-quit`、`render-process-gone`、`uncaughtException`、
    `unhandledRejection` 已接入；重复恢复调度、端口冲突等问题已在
    前置提交中处理（fea7194 等）。
- 待补测：
  - 真实桌面环境下的人工验收（崩溃对话框、自动恢复行为）仍未产出证据，
    归入 TP-10 桌面级人机验收与 TP-08 联合补测。

### TP-09 统一导航弹窗空错态与隐藏页副作用

- 状态：**主要完成**
- 证据：
  - 主导航一级/二级菜单切换、隐藏页副作用裁剪等在 959353b 检查点前
    已完成，对应自动化在 `tests/renderer/App.navigation.test.tsx` 等。
- 已知 flaky：
  - `App.navigation.test.tsx` 在 `npx vitest run` 全量并发下偶发
    DOM 查询多节点报错；在单文件 isolate 模式下 10/10 全通过。
    Review 轮次 3 已确认属预先存在的 RTL cleanup 行为与并发抖动，
    与本分支 TP 变更无因果关系。123b76f 通过提高 testTimeout
    显著缓解，但未彻底消除跨文件 DOM 隔离偏差。

## 5. 结论

- 按 TP 对照的“实装主链路 + 核心自动化”均已交付并在 `npx vitest run`
  中 98/98 文件、1666/1666 测试 GREEN。
- `npm run build` 三段式构建全部成功。
- 已显式列出的待补测项（TP-01 单任务 cancel、TP-03 批量 md5、TP-08
  真实桌面验收、TP-09 跨文件 DOM 隔离）均为后续切片范围，不构成本
  次交付的功能阻塞。
- 分支 `feat/refactor-todo-full` 已推送到 origin，未合并到 master，
  符合 TODO.md 中“基于 master 新建单独分支进行开发 / 推送所有更改 /
  不合并分支”的交付边界。
