# TP-10 建立桌面基础能力：后台常驻、托盘、通知基础 - 测试记录

## 1. 记录目的

本记录用于承接 `重构文档/测试用例/TP-10-建立桌面基础能力后台常驻托盘通知基础.md` 中要求的实际执行结果、证据位置、结论状态、执行环境与 source of truth。

当前记录先如实沉淀 **已完成的自动化回归证据**，并把 **尚未完成的人机验证项** 明确标记为 `待补测`，避免把“代码已落地”误写成“TP-10 已整体通过”。

## 2. 执行环境

- 执行时间：2026-04-15
- 工作目录：`M:\yande\yande-gallery-desktop\.worktrees\refactor-todo-full`
- 分支：`feat/refactor-todo-full`
- 提交号：`e5e9e098a835f603ba26d998b47ff445bbab6546`
- 平台：Windows 10 Home China 10.0.19045
- Shell：bash
- 测试命令：
  ```bash
  npm test -- --run tests/main/index.tray.test.ts tests/main/index.lifecycle.test.ts tests/main/index.startup.test.ts tests/main/index.appProtocol.test.ts tests/main/window.security.test.ts tests/main/services/bulkDownloadService.notifications.test.ts
  ```
- 自动化结果摘要：6 个测试文件、36 个测试全部通过

## 3. Source of truth

本次记录采用以下 source of truth：

1. 测试设计文档：`重构文档/测试用例/TP-10-建立桌面基础能力后台常驻托盘通知基础.md`
2. 主进程运行模型：`src/main/index.ts`
3. 主窗口 / tray / 恢复入口：`src/main/window.ts`
4. 通知统一触发层：`src/main/services/bulkDownloadService.ts`
5. 本次自动化证据输出：`重构文档/测试记录/TP-10-自动化回归输出.txt`
6. 本次回归用例：
   - `tests/main/index.tray.test.ts`
   - `tests/main/index.lifecycle.test.ts`
   - `tests/main/index.startup.test.ts`
   - `tests/main/window.security.test.ts`
   - `tests/main/services/bulkDownloadService.notifications.test.ts`

## 4. 本次已验证的支撑事实

### 4.1 自动化已覆盖的能力

- 主进程已创建 tray，并提供“恢复主窗口 / 退出应用”入口
- tray 点击恢复、显式退出、tray 初始化失败降级、无效 tray 引用兜底已由测试覆盖
- `before-quit` / `will-quit` 已进入统一清理链路，并避免重复清理
- `window-all-closed` 在 tray 可用时不直接退出；tray 不可用时会直接退出，避免留下无入口后台实例
- `render-process-gone` / `uncaughtException` / `unhandledRejection` 已接入统一治理入口
- 桌面通知已经由主进程统一触发，并支持点击后恢复 / 重建主窗口
- `window.ts` 已具备 close-to-tray、通知恢复、隐藏窗口 show/focus、无主窗口时重建、避免误用子窗口等能力

### 4.2 本次未完成的人机验证

以下能力在本次记录中 **没有** 以真实桌面环境人工观察完成，因此不得外推为 TP-10 整体通过：

- 系统托盘图标真实可见、可点击、不会被系统吞掉
- 真实系统通知弹出与点击体验
- 用户从右上角关闭按钮触发时的真实桌面交互体验
- second-instance 在真实打包 / 启动入口上的恢复体验
- 真实长任务在窗口关闭后继续运行的桌面级连续性

## 5. 用例执行记录

| 用例 | 实际结果 | 结论 | 证据路径 / 附件名 |
|---|---|---|---|
| TP-10-TC-001 关闭主窗口不应默认等价于退出应用 | 自动化已验证 `window-all-closed` 在 tray 可用场景不会直接 `app.quit()`，`window.ts` 主窗口 close 在非显式退出时会转为隐藏；但未做人机验证确认真实关闭按钮体验与托盘可见性 | 待补测 | `tests/main/index.lifecycle.test.ts`、`tests/main/window.security.test.ts`、`重构文档/测试记录/TP-10-自动化回归输出.txt` |
| TP-10-TC-002 显式退出入口应真正结束应用并清理桌面承载面 | 自动化已验证 tray 显式退出入口会进入统一 quit 清理链路；但未在真实桌面环境确认 tray 图标清理与进程退出观察 | 待补测 | `tests/main/index.tray.test.ts`、`tests/main/index.startup.test.ts`、`重构文档/测试记录/TP-10-自动化回归输出.txt` |
| TP-10-TC-003 tray 应形成可复测的恢复主窗口入口 | 自动化已验证 tray 点击会走统一主窗口恢复入口；但未人工确认真实托盘动作、焦点恢复与系统承载面行为 | 待补测 | `tests/main/index.tray.test.ts`、`重构文档/测试记录/TP-10-自动化回归输出.txt` |
| TP-10-TC-004 主窗口已销毁时 tray 或 second-instance 应能重建窗口 | 自动化已验证通知恢复和统一恢复入口在无现存主窗口时会创建新主窗口，并避免误用子窗口；second-instance 的真实注入路径未做人机验证 | 待补测 | `tests/main/window.security.test.ts`、`tests/main/index.lifecycle.test.ts`、`重构文档/测试记录/TP-10-自动化回归输出.txt` |
| TP-10-TC-005 长任务进行中关闭窗口后应继续运行 | 当前仅有 close-to-tray / quit 链路自动化证据；缺少“真实长任务运行中关闭窗口 -> 后台继续推进 -> 恢复观察”的桌面级验证 | 待补测 | `tests/main/index.lifecycle.test.ts`、`tests/main/index.startup.test.ts` |
| TP-10-TC-006 second-instance 到来时应唤醒已有窗口或重建主窗口 | 自动化已验证 second-instance 会走统一主窗口恢复入口，但未在真实启动入口上完成复测 | 待补测 | `tests/main/index.lifecycle.test.ts`、`重构文档/测试记录/TP-10-自动化回归输出.txt` |
| TP-10-TC-007 通知设置必须真实控制通知行为 | 自动化已验证 `notifications` 开关会控制主进程是否发送桌面通知；但未完成真实系统通知的人机观察 | 待补测 | `tests/main/services/bulkDownloadService.notifications.test.ts`、`重构文档/测试记录/TP-10-自动化回归输出.txt` |
| TP-10-TC-008 成功 / 失败 / 需人工处理通知应与任务真实结果一致 | 自动化已验证 `completed / failed / allSkipped` 三类终态通知映射逻辑；但缺少真实系统通知展示与用户可见文案复核 | 待补测 | `tests/main/services/bulkDownloadService.notifications.test.ts`、`重构文档/测试记录/TP-10-自动化回归输出.txt` |
| TP-10-TC-009 点击通知后的恢复行为必须可解释 | 自动化已验证通知点击会走统一恢复入口，并可 show/focus 已隐藏窗口或重建主窗口；未做系统通知点击的人机验证 | 待补测 | `tests/main/services/bulkDownloadService.notifications.test.ts`、`tests/main/window.security.test.ts`、`重构文档/测试记录/TP-10-自动化回归输出.txt` |
| TP-10-TC-010 托盘菜单应提供最小可理解动作集合 | 自动化已验证 tray 菜单包含恢复主窗口与显式退出入口；未完成人机验证确认真实文案与状态查看动作策略 | 待补测 | `tests/main/index.tray.test.ts`、`重构文档/测试记录/TP-10-自动化回归输出.txt` |
| TP-10-TC-011 关闭到托盘的重复操作必须幂等 | 自动化已验证 tray 初始化失败降级、失效 tray 引用退出兜底、统一恢复入口不误用子窗口；但未在真实桌面环境完成重复 close/hide/show 幂等复测 | 待补测 | `tests/main/index.tray.test.ts`、`tests/main/window.security.test.ts` |
| TP-10-TC-012 退出后再次启动应恢复到明确初始桌面形态 | 本次未执行真实桌面冷启动 / 退出后重启观察；仅有统一启动与退出治理自动化支撑 | 待补测 | `tests/main/index.startup.test.ts` |

## 6. 当前结论

1. **TP-10 的代码级主链路已经具备明确自动化支撑**：tray、close-to-tray、统一 quit 清理、通知统一触发、通知点击恢复、统一恢复入口、异常治理等关键分支均已有回归测试。
2. **TP-10 仍不能在本记录中判定“通过”**：因为测试设计文档明确要求 tray / 系统通知 / 窗口关闭恢复等桌面行为必须保留人机验证证据，而本次记录尚未完成这些人工观察。
3. 因此，本次 TP-10 结论应保持为：
   - 代码实现：已补齐基础能力
   - 自动化支撑：已建立
   - 阶段验收：**待补测**

## 7. 后续补测清单

- 在真实桌面环境录制以下证据：
  - `TP-10-TC-001-关闭窗口后常驻录屏.mp4`
  - `TP-10-TC-003-tray恢复窗口录屏.mp4`
  - `TP-10-TC-006-second-instance唤醒录屏.mp4`
  - `TP-10-TC-007-通知开关前后对比录屏.mp4`
  - `TP-10-TC-012-退出后重启桌面形态录屏.mp4`
- 在真实长任务样本下补充关闭窗口后的后台持续运行证据
- 完成后再更新本记录中的结论字段，不得以当前自动化结果直接替代桌面级验收