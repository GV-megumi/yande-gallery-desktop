# 图库懒加载设计

## 核心原则

图库列表先展示元数据，图片扫描和导入按需触发，不在应用启动时把所有目录一次性扫完。

## 这样设计的原因

- 大图库启动扫描会明显拖慢应用进入速度。
- 图库页更适合先给用户可浏览的结构，再逐步加载内容。
- 封面和缩略图优先展示，可以减少初次渲染成本。

## 设计要点

- `galleries` 表存目录元数据和缓存统计。
- 真正的图片导入在用户进入图库或触发扫描时进行。
- 图集封面优先使用缩略图，而不是原图。

## 相关模块

- `src/main/services/galleryService.ts`
- `src/main/services/imageService.ts`
- `src/main/services/thumbnailService.ts`
- `src/renderer/pages/GalleryPage.tsx`
